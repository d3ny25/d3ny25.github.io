---
---


<form
  id="contactForm"
  class="bg-white shadow-xl rounded-lg p-8 max-w-xl mx-auto border border-gray-200"
>
  <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
    Contáctame 📬
  </h2>

  <div class="mb-4">
    <label for="name" class="block text-gray-700 font-semibold mb-2">Nombre</label>
    <input
      type="text"
      id="name"
      name="name"
      required
      class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none"
      placeholder="Tu nombre completo"
    />
  </div>

  <div class="mb-4">
    <label for="email" class="block text-gray-700 font-semibold mb-2">Gmail</label>
    <input
      type="email"
      id="email"
      name="email"
      required
      class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none"
      placeholder="tuemail@gmail.com"
    />
  </div>

  <div class="mb-4">
    <label for="phone" class="block text-gray-700 font-semibold mb-2">Teléfono</label>
    <input
      type="tel"
      id="phone"
      name="phone"
      required
      class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none"
      placeholder="+52 123 456 7890"
    />
  </div>

  <div class="mb-6">
    <label for="message" class="block text-gray-700 font-semibold mb-2">Asunto o Mensaje</label>
    <textarea
      id="message"
      name="message"
      rows="4"
      required
      class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none"
      placeholder="Escribe tu mensaje aquí..."
    ></textarea>
  </div>

  <div class="text-center">
    <button
      type="submit"
      class="bg-linear-to-r from-blue-600 to-indigo-600 text-white px-8 py-3 rounded-lg font-semibold hover:scale-105 hover:shadow-lg transition-all duration-200"
    >
      Enviar mensaje
    </button>
  </div>

  <p id="statusMessage" class="text-center mt-4 text-gray-600 hidden">
    ✅ Formulario enviado con éxito.
  </p>
</form>

<!-- 🧠 Script del lado del cliente -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const form = document.getElementById("contactForm") as HTMLFormElement | null;
    const statusMessage = document.getElementById("statusMessage") as HTMLElement | null;

    if (!form || !statusMessage) return; // ✅ Evita errores si no existen

    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      const data = new FormData(form);
      console.log("Formulario enviado:", Object.fromEntries(data.entries()));

      // Aquí podrías conectar con Google Sheets con fetch() si quieres hacerlo funcional

      // Mostrar mensaje de éxito
      statusMessage.classList.remove("hidden");
      statusMessage.textContent = "✅ Formulario enviado con éxito.";

      // Limpiar campos
      form.reset(); // ✅ Ya no marca error porque form está tipado correctamente
    });
  });
</script>
